services:
  - type: web
    runtime: static
    name: alderwell-pharmaceutical
    rootDir: react-frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
